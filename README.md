# RedEngine documentation site

This directory is generated from the package's `Documentation~` sources.

```sh
npm run docs:dev
npm run docs:build
npm run docs:preview
```

Run `Tools~/Documentation/sync-and-build.sh` from the RedEngine checkout to refresh the sources and
install the latest VitePress release. Edit the package documentation, not the generated copy.
